# Azure Static Web Apps is blocked on Azure for Students subscriptions.
# Frontend is deployed on Vercel (free) instead — see frontend/ChatWidget.tsx.
# Vercel deployment: cd into frontend repo → `vercel deploy`
