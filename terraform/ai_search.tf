# Azure AI Search is blocked on Azure for Students subscriptions.
# The chatbot uses local FAISS instead — no changes needed to the app code,
# it already falls back to FAISS when AZURE_SEARCH_ENDPOINT is not set.
